# ross_momentum_system

Minimal Ross-style momentum trading system scaffold.

Structure mirrors a production-like paper-trading loop: scan → select → monitor → trade → evaluate.

Run quick smoke test:

```bash
python run/run_paper_trading.py
```
# ross_scalp
multi symbol top performer scanner and live trading engine for momentum breakout scalping
